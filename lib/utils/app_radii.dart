/*
 * Najaz Mobile App
 * Border Radius System
 * 
 * This file contains all border radius constants used throughout the app.
 * Border radius values extracted from Figma designs.
 */

/// App Radii - All border radius constants for Najaz App
/// 
/// Values extracted from Figma SVG designs.
class AppRadii {
  // ============================================================
  // BORDER RADIUS VALUES (from Figma)
  // ============================================================
  
  /// Screen container radius: 18dp (from rx="18" in designs)
  static const double screenContainer = 18.0;
  
  /// Button border radius: 16dp (from rx="16" in designs)
  static const double button = 16.0;
  
  /// Input field border radius: 7dp (from rx="7" in designs)
  static const double input = 7.0;
  
  /// Card border radius: 18dp (from rx="18" in designs)
  static const double card = 18.0;
  
  /// Dialog/Outer container radius: 22dp (from rx="22" in designs)
  static const double dialog = 22.0;
  
  /// Badge/Small element radius: 2dp (from rx="2" in designs)
  static const double badge = 2.0;
  
  /// Keyboard key radius (small): 6dp (from rx="6" in designs)
  static const double keyboardKey = 6.0;
  
  /// Keyboard key radius (large/rounded): 23dp (from rx="23" in designs)
  static const double keyboardKeyLarge = 23.0;
  
  /// Circular element radius (e.g., avatar): 13dp (half of 26 from rx="13" in designs)
  static const double circular = 13.0;
  
  /// Datepicker container radius: 10dp (from rx="10.2358" in Datepickers.svg)
  static const double datepicker = 10.0;
  
  /// Calendar day cell radius: 5dp (from rx="5.28302" in Datepickers.svg)
  static const double calendarDay = 5.0;
  
  /// Bottom navigation bar radius: 30dp (from rx="30" in Home Page, Services Page, etc.)
  static const double bottomNavBar = 30.0;
  
  /// Image container radius: 8dp (from rx="8" in Home Page, Services Page)
  static const double imageContainer = 8.0;
  
  // ============================================================
  // SEMANTIC RADIUS VALUES
  // ============================================================
  
  /// Standard border radius for most UI elements
  static const double standard = 8.0;
  
  /// Small border radius for subtle rounding
  static const double small = 4.0;
  
  /// Medium border radius (between small and standard)
  static const double medium = 5.0;
  
  /// Large border radius for prominent rounding
  static const double large = 16.0;
}


